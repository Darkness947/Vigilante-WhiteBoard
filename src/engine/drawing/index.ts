export { smoothStroke, movingAverageSmooth, type SmoothingConfig } from './StrokeSmoother';
export {
    renderStroke,
    renderShape,
    renderAll,
    clearCanvas,
    type RenderConfig
} from './CanvasRenderer';
